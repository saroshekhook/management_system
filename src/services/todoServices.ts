/* // src/services/databaseService.ts
import { Todo, Attribute } from "../models";

export const getAllTables = async () => {
  // Retrieve all tables from the database
};

export const createTable = async (tableData: any) => {
  // Create a new table in the database
};

export const updateTable = async (tableId: number, tableData: any) => {
  // Update a table in the database
};

export const deleteTable = async (tableId: number) => {
  // Delete a table from the database
};

export const createAttribute = async (tableId: number, attributeData: any) => {
  // Create a new attribute for a table
};

export const updateAttribute = async (
  tableId: number,
  attributeId: number,
  attributeData: any
) => {
  // Update an attribute of a table
};

export const deleteAttribute = async (tableId: number, attributeId: number) => {
  // Delete an attribute of a table
};
 */